<ul class="nav flex-column">
  <%= nav_item 'Home', root_path, icon_name: 'home' %>
</ul>

<hr class="mt-1" />

<h6 class="sidebar-heading">
  <%= link_to 'New app', new_app_path, class: 'btn btn-sm btn-primary float-right' %>
  <%= link_to 'Apps', apps_path %>
</h6>
<ul class="nav flex-column mb-3">
  <% App.order(:name).each do |app| %>
    <%= nav_item app.name, app_path(app), icon_name: 'cube' %>
  <% end %>
</ul>
