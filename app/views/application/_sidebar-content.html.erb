<ul class="nav flex-column">
  <%= nav_item 'Home', root_path, icon_name: 'home' %>
</ul>

<hr class="mt-1" />

<h6 class="sidebar-heading">
  <%= link_to 'New project', new_project_path, class: 'btn btn-sm btn-primary float-right' %>
  <%= link_to 'Projects', projects_path %>
</h6>
<ul class="nav flex-column mb-3">
  <% Project.order(:name).each do |project| %>
    <%= nav_item project.name, project_path(project), icon_name: 'cube' %>
  <% end %>
</ul>

<hr class="mt-1" />

<h6 class="sidebar-heading">
  <%= link_to 'Users', users_path %>
</h6>
